server:
  port: 8080

spring:
  application:
    name: achievement-service
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://postgres:5432/nutrilens}
    username: ${DATABASE_USERNAME:nutrilens}
    password: ${DATABASE_PASSWORD:nutrilens}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: gamification
        jdbc.time_zone: UTC
  flyway:
    schemas: gamification
    default-schema: gamification
    table: flyway_schema_history_achievement
    locations: classpath:db/migration
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:29092}
    consumer:
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: ${spring.application.name}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
